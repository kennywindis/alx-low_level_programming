#include "main.h"

/**
 * _abs - prints_absolute value of an integer
 * @n: n is an integer
 * Return: always 0
 */

int _abs(int n)
{
if (n > 0)

return (n);

else if (n < 0)

return (-n);

else

return (0);
}

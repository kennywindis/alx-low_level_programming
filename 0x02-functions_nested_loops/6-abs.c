#include "main.h"

/**
 * _abs - prints_absolute value of an integer
 * @n: n is an integer
 * Return: always 0
 */

int _abs(int n)
{
if (n > 0)

return (1);

else if (n < 0)

return (-1);

else

return (0);
}

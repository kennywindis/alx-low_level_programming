#include <stdlib.h>
#include <time.h>
#include <stdio.h>


/**
 * main - prints the alphabet in lowercase
 * Return: 0 if (Success)
 */

int main(void)

{
char d = 'a';
while (d <= 'a')
{
putchar(d);
d++;
}
putchar('\n);
return (0);
}
